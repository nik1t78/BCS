(restart-windows.bat is a source/config file, and there are no build artifacts, dependencies, or temp files in the changes - only an empty array for added/deleted files and a batch file modification)

(empty - output nothing)